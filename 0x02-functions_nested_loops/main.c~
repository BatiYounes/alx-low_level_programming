#include "main.h"
#include <unistd.h>

int _putchar(char c);

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    print_alphabet();
    return (0);
}

int _putchar(char c)
{
    return write(1, &c, 1);
}
